{
  "name": "edge-report",
  "version": "2.0.0",
  "description": "NHL Edge Stats",
  "scripts": {
    "build:css": "tailwindcss -i ./frontend/src/input.css -o ./frontend/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.2"
  }
}
